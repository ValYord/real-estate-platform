import HeroSearch from '@/components/home/HeroSearch'

export default function HomePage() {
  return (
    <main>
      <HeroSearch />
      {/* Below-the-fold sections (QuickCards, FeaturedCarousel, BrowseByCity, etc.) — next task */}
    </main>
  )
}
